**What does this change?**

A short summary, and a link to the related issue if there is one.

**How did you test it?**

**Checklist**

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` succeeds
- [ ] If this adds a data source: it's registered in `src/ingestion/pipeline.ts`, documented in `docs/data-sources.md`, and a second `npm run ingest` adds no duplicates
